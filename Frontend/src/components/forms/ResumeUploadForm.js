

function ResumeUploadForm({ hidden }) {
    return (
        <div className={`${hidden ? 'hidden' : 'mx-4 my-4'}`}>
            Resume Upload Form!
        </div>
    )
}


export default ResumeUploadForm;