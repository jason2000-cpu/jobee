
function CertificationsForm({ hidden }) {
    return (
        <div className={`${hidden ? 'hidden' : 'mx-4 my-4'}`}>
            Certifications Form!
        </div>
    )
}

export default CertificationsForm;